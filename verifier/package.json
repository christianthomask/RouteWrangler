{
  "name": "@routewrangler/verifier",
  "version": "1.0.0",
  "private": true,
  "description": "Automated UAT: drives the compiled API and the simulator against a scratch database, end to end.",
  "scripts": {
    "uat": "tsx src/main.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint \"src/**/*.ts\""
  },
  "dependencies": {
    "@routewrangler/contracts": "workspace:*",
    "@routewrangler/simulator": "workspace:*",
    "postgres": "^3.4.5"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "eslint": "^9.18.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}
