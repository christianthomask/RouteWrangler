import { Dashboard } from '@/components/Dashboard';

// Sprint 0 shell. The field PWA (runs, stops, offline capture) lands in Sprint 3.
export default function FieldPage() {
  return <Dashboard requiredRole="reader" title="Field" />;
}
