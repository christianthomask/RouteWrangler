/**
 * Product identity in one place (DESIGN_BRIEF §3). Name is provisionally
 * "Verameter" (ADR-017) — "the true measure" (vera + meter). Repo/package
 * identifiers stay `routewrangler` regardless; this is the display brand only,
 * swappable in a single edit across every surface (wordmark, <title>, login).
 */
export const PRODUCT_NAME = 'Verameter';

/** One-line descriptor shown under the wordmark on the login page. */
export const PRODUCT_DESCRIPTOR = 'Meter data management for contract water-meter reading';
