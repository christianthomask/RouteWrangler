/**
 * Product identity in one place (DESIGN_BRIEF §3). The final name is a design
 * deliverable pending sign-off; until then this holds the working name so every
 * surface (wordmark, <title>, login) swaps in a single edit. See
 * docs/decisions/ADR-017 once the name is chosen.
 */
export const PRODUCT_NAME = 'RouteWrangler';

/** One-line descriptor shown under the wordmark on the login page. */
export const PRODUCT_DESCRIPTOR = 'Meter data management for contract water-meter reading';
