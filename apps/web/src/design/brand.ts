/**
 * Product identity in one place (DESIGN_BRIEF §3). Name is provisionally
 * "Freshet" (ADR-017) — pending trademark clearance. Repo/package identifiers
 * stay `routewrangler` regardless; this is the display brand only, swappable in
 * a single edit across every surface (wordmark, <title>, login).
 */
export const PRODUCT_NAME = 'Freshet';

/** One-line descriptor shown under the wordmark on the login page. */
export const PRODUCT_DESCRIPTOR = 'Meter data management for contract water-meter reading';
